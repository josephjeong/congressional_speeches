from datetime import datetime
from typing import Optional
import pandas as pd

def frequency_bound(word: Optional[str], start_year: Optional[datetime], end_year: Optional[datetime]) -> pd.DataFrame:

    return pd.DataFrame()

